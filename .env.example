# Vite picks these up at build time for the browser bundle.
# The publishable key is designed to be public — RLS on the Supabase
# side scopes reads to auth.uid().
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_YOUR-KEY
